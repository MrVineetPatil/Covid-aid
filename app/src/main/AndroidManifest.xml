<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moutamid.covidaid">

    <application
        android:allowBackup="true"
        android:icon="@drawable/broadcast"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomApiProject">
        <activity android:name=".Analyse4"></activity>
        <activity android:name=".Analyse3" />
        <activity android:name=".Analyse2" />
        <activity android:name=".Analyse1" />
        <activity android:name=".PredictionPage" />
        <activity android:name=".Visualisation" />
        <activity android:name=".FormActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityLogin" />
        <activity android:name=".ActivitySignUp" />
    </application>

</manifest>